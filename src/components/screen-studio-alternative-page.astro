---
import "../styles/global.css";
import { appStoreUrl, localizedPath, siteUrl } from "../lib/i18n";
import type { Locale } from "../lib/i18n";
import { getScreenStudioAlternativeCopy } from "../lib/screen-studio-alternative";
import { getTranslation } from "../lib/translations";
import { FAQSection } from "./faq-section";
import { Footer } from "./footer";
import PageHead from "./page-head.astro";
import { SiteHeader } from "./site-header";
import { SmoothScroll } from "./smooth-scroll";

interface Props {
  locale: Locale;
}

const { locale } = Astro.props;
const t = getTranslation(locale);
const copy = getScreenStudioAlternativeCopy(locale);
const path = "/screen-studio-alternative";
const pageUrl = `${siteUrl}${localizedPath(locale, path)}`;
const screenStudioUrl = "https://screen.studio/";
const benchmarkUrl = "https://benchmark.thescreen.cam";

const structuredData = {
  "@context": "https://schema.org",
  "@graph": [
    {
      "@type": "WebPage",
      "@id": `${pageUrl}#webpage`,
      url: pageUrl,
      name: copy.meta.title,
      description: copy.meta.description,
      dateModified: "2026-06-13",
      inLanguage: locale,
      about: copy.schema.about.map((name) => ({ "@type": "Thing", name })),
      mentions: [
        { "@type": "SoftwareApplication", name: "Screen Studio", url: screenStudioUrl },
        { "@id": `${siteUrl}/#software` },
      ],
    },
    {
      "@type": "BreadcrumbList",
      "@id": `${pageUrl}#breadcrumb`,
      itemListElement: [
        {
          "@type": "ListItem",
          position: 1,
          name: "ScreenCam",
          item: siteUrl,
        },
        {
          "@type": "ListItem",
          position: 2,
          name: copy.schema.breadcrumbName,
          item: pageUrl,
        },
      ],
    },
    {
      "@type": "SoftwareApplication",
      "@id": `${siteUrl}/#software`,
      name: "ScreenCam",
      applicationCategory: "MultimediaApplication",
      operatingSystem: "macOS 15+",
      url: siteUrl,
      image: `${siteUrl}/og.jpg`,
      downloadUrl: appStoreUrl,
      description: copy.schema.softwareDescription,
      offers: {
        "@type": "Offer",
        price: "0",
        priceCurrency: "USD",
        url: appStoreUrl,
      },
      featureList: copy.schema.featureList,
    },
    {
      "@type": "FAQPage",
      "@id": `${pageUrl}#faq`,
      mainEntity: copy.faq.items.map((item) => ({
        "@type": "Question",
        name: item.question,
        acceptedAnswer: {
          "@type": "Answer",
          text: item.answer,
        },
      })),
    },
  ],
};
---

<!doctype html>
<html lang={locale} class="dark">
  <head>
    <PageHead
      locale={locale}
      path={path}
      title={copy.meta.title}
      description={copy.meta.description}
      robots="index, follow, max-image-preview:large"
      structuredData={structuredData}
    />
  </head>
  <body class="bg-background font-sans text-foreground antialiased">
    <SmoothScroll client:media="(hover: hover) and (pointer: fine)" />
    <a class="skip-link" href="#main">{t.common.skipToContent}</a>
    <SiteHeader
      copy={t.header}
      currentPath={path}
      languagePath={path}
      languageLabel={t.footer.language}
      locale={locale}
      client:load
    />

    <main id="main">
      <section class="relative min-h-[82vh] overflow-hidden bg-background">
        <img
          src="/og.jpg"
          alt={copy.imageAlt}
          class="absolute inset-0 h-full w-full object-cover opacity-28"
          loading="eager"
        />
        <div class="absolute inset-0 bg-[linear-gradient(90deg,rgba(5,5,5,0.97)_0%,rgba(5,5,5,0.82)_46%,rgba(5,5,5,0.58)_100%)]"></div>
        <div class="relative z-10 mx-auto flex min-h-[82vh] max-w-7xl flex-col justify-end px-6 pb-12 pt-32 md:px-12 md:pb-16">
          <p class="mb-5 text-xs font-medium uppercase tracking-[0.2em] text-foreground/70">
            {copy.hero.eyebrow}
          </p>
          <h1 class="max-w-4xl text-[clamp(3rem,7.2vw,6.25rem)] font-bold leading-[0.92] tracking-tight text-foreground text-balance">
            {copy.hero.title}
          </h1>
          <p class="mt-7 max-w-2xl text-base leading-7 text-foreground/76 md:text-lg">
            {copy.hero.description}
          </p>
          <div class="mt-8 grid max-w-4xl grid-cols-2 gap-x-6 gap-y-5 border-y border-foreground/14 py-5 md:grid-cols-4">
            {copy.performanceHighlights.map((item) => (
              <div>
                <p class="text-2xl font-semibold tracking-tight text-foreground md:text-3xl">{item.value}</p>
                <p class="mt-1 text-xs uppercase tracking-[0.14em] text-foreground/55">{item.label}</p>
              </div>
            ))}
          </div>
          <div class="mt-10 flex flex-col gap-3 sm:flex-row">
            <a class="inline-flex w-fit items-center justify-center bg-foreground px-5 py-3 text-sm font-medium text-background transition-colors hover:bg-foreground/90" href={appStoreUrl} target="_blank" rel="noopener noreferrer">
              {copy.hero.appStoreCta}
            </a>
            <a class="inline-flex w-fit items-center justify-center border border-foreground/30 px-5 py-3 text-sm font-medium text-foreground transition-colors hover:border-foreground/70" href={benchmarkUrl} target="_blank" rel="noopener noreferrer">
              {copy.hero.benchmarkCta}
            </a>
          </div>
          <p class="mt-8 text-xs text-foreground/50">{copy.hero.lastUpdatedLabel} {copy.lastUpdated}</p>
        </div>
      </section>

      <section class="bg-background px-6 py-20 md:px-12" aria-labelledby="why-switch-title">
        <div class="mx-auto max-w-7xl">
          <div class="grid gap-10 md:grid-cols-[0.82fr_1.18fr] md:items-start">
            <div>
              <p class="mb-4 text-xs font-medium uppercase tracking-[0.2em] text-muted-foreground">{copy.whySwitch.eyebrow}</p>
              <h2 id="why-switch-title" class="text-3xl font-semibold leading-tight tracking-tight text-foreground md:text-5xl">
                {copy.whySwitch.title}
              </h2>
              <p class="mt-5 text-base leading-7 text-muted-foreground">
                {copy.whySwitch.description}
              </p>
            </div>
            <div class="grid gap-3 sm:grid-cols-2">
              {copy.performanceHighlights.map((item) => (
                <article class="border border-border/70 bg-card/25 p-5">
                  <div class="flex items-baseline justify-between gap-4">
                    <h3 class="text-lg font-semibold tracking-tight text-foreground">{item.value}</h3>
                    <p class="text-right text-xs uppercase tracking-[0.14em] text-muted-foreground">{item.label}</p>
                  </div>
                  <p class="mt-4 text-sm leading-6 text-muted-foreground">{item.detail}</p>
                </article>
              ))}
            </div>
          </div>
        </div>
      </section>

      <section class="bg-secondary/25 px-6 py-20 md:px-12" aria-labelledby="feature-title">
        <div class="mx-auto max-w-7xl">
          <div class="mb-12 max-w-4xl">
            <p class="mb-4 text-xs font-medium uppercase tracking-[0.2em] text-muted-foreground">{copy.features.eyebrow}</p>
            <h2 id="feature-title" class="text-3xl font-semibold leading-tight tracking-tight text-foreground md:text-5xl">
              {copy.features.title}
            </h2>
          </div>
          <div class="grid gap-3 md:grid-cols-2 lg:grid-cols-3">
            {copy.features.items.map((feature) => (
              <article class="border border-border/70 bg-background/50 p-5">
                <h3 class="text-lg font-semibold tracking-tight text-foreground">{feature.title}</h3>
                <p class="mt-3 text-sm leading-6 text-muted-foreground">{feature.body}</p>
              </article>
            ))}
          </div>
        </div>
      </section>

      <section class="bg-background px-6 py-20 md:px-12" aria-labelledby="comparison-title">
        <div class="mx-auto max-w-7xl">
          <div class="mb-12 max-w-4xl">
            <p class="mb-4 text-xs font-medium uppercase tracking-[0.2em] text-muted-foreground">{copy.comparison.eyebrow}</p>
            <h2 id="comparison-title" class="text-3xl font-semibold leading-tight tracking-tight text-foreground md:text-5xl">
              {copy.comparison.title}
            </h2>
            <p class="mt-5 text-base leading-7 text-muted-foreground">
              {copy.comparison.description}
            </p>
          </div>

          <div class="overflow-x-auto border border-border/70">
            <table class="w-full min-w-[780px] border-collapse text-left text-sm">
              <thead class="bg-secondary/40 text-xs uppercase tracking-[0.18em] text-muted-foreground">
                <tr>
                  <th class="w-[20%] border-b border-border/70 px-5 py-4 font-medium">{copy.comparison.headers.feature}</th>
                  <th class="w-[40%] border-b border-border/70 px-5 py-4 font-medium">{copy.comparison.headers.screenCam}</th>
                  <th class="w-[40%] border-b border-border/70 px-5 py-4 font-medium">{copy.comparison.headers.screenStudio}</th>
                </tr>
              </thead>
              <tbody>
                {copy.comparison.rows.map((row) => (
                  <tr class="border-b border-border/60 last:border-b-0">
                    <th class="align-top px-5 py-4 text-sm font-semibold text-foreground">{row.feature}</th>
                    <td class="align-top px-5 py-4 leading-6 text-muted-foreground">{row.screenCam}</td>
                    <td class="align-top px-5 py-4 leading-6 text-muted-foreground">{row.screenStudio}</td>
                  </tr>
                ))}
              </tbody>
            </table>
          </div>
        </div>
      </section>

      <section class="bg-secondary/25 px-6 py-20 md:px-12" aria-labelledby="use-cases-title">
        <div class="mx-auto grid max-w-7xl gap-4 md:grid-cols-2">
          <div>
            <p class="mb-4 text-xs font-medium uppercase tracking-[0.2em] text-muted-foreground">{copy.bestFit.eyebrow}</p>
            <h2 id="use-cases-title" class="text-3xl font-semibold leading-tight tracking-tight text-foreground md:text-5xl">
              {copy.bestFit.title}
            </h2>
            <ul class="mt-8 grid gap-3">
              {copy.bestFit.items.map((item) => (
                <li class="border border-border/70 bg-background/70 p-4 text-sm leading-6 text-foreground">{item}</li>
              ))}
            </ul>
          </div>
          <div class="border border-border/70 bg-background/70 p-6 md:p-8">
            <p class="mb-4 text-xs font-medium uppercase tracking-[0.2em] text-muted-foreground">{copy.bestFit.gapEyebrow}</p>
            <h3 class="text-2xl font-semibold tracking-tight text-foreground">{copy.bestFit.gapTitle}</h3>
            <p class="mt-5 text-sm leading-6 text-muted-foreground">
              {copy.bestFit.gapDescription}
            </p>
            <div class="mt-8 flex flex-col gap-3 sm:flex-row">
              <a class="inline-flex justify-center bg-foreground px-5 py-3 text-sm font-medium text-background transition-colors hover:bg-foreground/90" href={appStoreUrl} target="_blank" rel="noopener noreferrer">
                {copy.bestFit.pricingCta}
              </a>
              <a class="inline-flex justify-center border border-border/80 px-5 py-3 text-sm font-medium text-foreground transition-colors hover:border-foreground/60" href={screenStudioUrl} target="_blank" rel="noopener noreferrer">
                {copy.bestFit.screenStudioCta}
              </a>
            </div>
          </div>
        </div>
      </section>

      <FAQSection copy={copy.faq} client:visible={{ rootMargin: "-25% 0px" }} />
    </main>

    <Footer copy={t.footer} locale={locale} client:visible={{ rootMargin: "-25% 0px" }} />
  </body>
</html>
